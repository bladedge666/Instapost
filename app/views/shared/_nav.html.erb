    <nav class="container-fluid">
      <div class="row">
        <div class = "col-lg-6 col-lg-offset-3">
          <div class="row">
            <div class="nav-wrapper">

              <div class="col-xs-6">
                <span class="pull-left brand"><%= link_to icon('instagram'), root_path, class: "fa-lg" %> &nbsp;  <%= link_to 'Instapost', root_path %></span>
              </div>

              <div class="col-xs-6">
                <% if user_signed_in? %>
                <span class="nav-links pull-right"><%= link_to icon('plus-circle'), new_post_path %></span>
                <span class="nav-links pull-right"><%= link_to icon('wrench'), edit_user_registration_path %></span>
                <span class="nav-links pull-right"><%= link_to icon('sign-out'), destroy_user_session_path, method: :delete, class: "fa-lg"  %></span>
                <% else %>
                <span class="nav-links pull-right"><%= link_to icon('user-plus'), new_user_registration_path, class: "fa-lg" %></span>
                <span class="nav-links pull-right"><%= link_to icon('sign-in'), new_user_session_path, class: "fa-lg"  %></span>
                <% end %>
              </div>

            </div>
          </div>
        </div>
      </div>
    </nav>